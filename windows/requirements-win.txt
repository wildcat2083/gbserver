pyboy
flask
flask-limiter
flask-sock
numpy
packaging
pyinstaller

# Optional: the Rust-based boytacean engine. No Windows wheel is published
# (every release on PyPI is source-only), so on Windows it must be compiled
# from source, which needs a Rust toolchain:
#   pip install setuptools-rust
#   pip install boytacean
# and then rebuild. Without it, the server runs on PyBoy only, which is
# fully supported. Uncomment the line below only if you have Rust installed.
# boytacean