# Normalize to LF in the repo and on checkout, even when cloned on Windows -
# systemd, nginx, sudoers, and shell all choke on stray \r characters.
* text=auto eol=lf

*.jpg binary
*.png binary
*.gb binary
*.gbc binary
*.state binary
*.sav binary
